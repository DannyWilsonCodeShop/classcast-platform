# DISABLED - Using ci-simple.yml instead
# This complex CI workflow was causing failures
# The simple CI workflow in ci-simple.yml is now active

# Original complex workflow has been disabled to prevent CI failures
# The new simple workflow only runs basic checks that are guaranteed to work